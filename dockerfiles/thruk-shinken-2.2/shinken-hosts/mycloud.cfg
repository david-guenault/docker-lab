define host {
  host_name                      mycloud
  alias                          mycloud
  address                        10.10.0.8
  use                            linux-ssh
  contact_groups                 admins
  _SSH_USER                      root
  service_excludes               NtpSync
}

