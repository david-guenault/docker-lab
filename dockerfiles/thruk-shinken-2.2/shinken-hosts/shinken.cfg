define host {
  host_name                      shinken
  address                        localhost
  use                            linux-ssh
  contact_groups                 admins
  service_excludes               NtpSync , Read-only Filesystems
}